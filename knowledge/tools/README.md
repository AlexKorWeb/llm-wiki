# tools/

Утилиты, библиотеки, плагины, сервисы, CLI, платформы — всё, что можно установить и использовать, **кроме** Claude Code Skills.

Признак: это внешний продукт (GitHub-репо, npm-пакет, SaaS, расширение), а не пакет `SKILL.md` для Claude Code.

Файлы — kebab-case `.md` с YAML frontmatter и URL-источниками сразу после заголовка.
